# Repository Instructions

- This workspace is for `meetaco/pybotters`.
- Never push branches to `pybotters/pybotters` from this repository.
- Never open pull requests targeting `pybotters/pybotters` from this repository.
- When a push or pull request is needed, use `meetaco/pybotters` as the target repository.
- If `meetaco/pybotters` cannot be used because of permission or auth issues, stop and ask the user. Do not create or use another fork unless the user explicitly requests it.
